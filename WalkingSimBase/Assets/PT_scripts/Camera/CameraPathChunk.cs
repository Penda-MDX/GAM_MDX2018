using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraPathChunk : MonoBehaviour {

    public GameObject[] nodeArray;
    
	// Use this for initialization
	void Start () {
        /*
        nodeArray = new GameObject[gameObject.transform.childCount];
        nodeCount = 0;
        for (int childCounter = 0; childCounter<gameObject.transform.childCount; childCounter++)
        {
            GameObject child = gameObject.transform.GetChild[childCounter].gameObject;
            if(child.tag == "Node")
            {
              nodeArray[nodeCount] = child;
              nodeCount++;
            }
        }
        */

	}
	
	// Update is called once per frame
	void Update () {
		
	}

}
